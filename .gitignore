# Local working notes / Claude Code handoff (private; remove this line to commit)
CLAUDE.md

# Private design memos / plans
plans/

# Private idea / TODO memos captured by the /idea command
idea/

# Environment-specific config sets (copied from config.example.json; one per project)
config.json
config.*.json
!config.example.json

# Dedicated Chrome debug profile
.chrome-debug-profile/

# Output logs (e.g. when output_dir is the default "logs")
logs/
*.log

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# OS / editor
Thumbs.db
desktop.ini
.DS_Store
.vscode/
.idea/

# Claude Code local settings (per-machine MCP/permissions)
.claude/settings.local.json
